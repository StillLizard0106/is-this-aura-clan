spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/canteenq
    username: your_user
    password: your_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
server:
  port: 8080
