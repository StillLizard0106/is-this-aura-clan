# Changelog

## [Unreleased]

- Added Spring Boot backend skeleton with `Task`, `Habit`, and `WellnessEntry` entities.
- Implemented service layer and repository layer for tasks, habits, wellness, and dashboard summary.
- Added API controller endpoints for core MVP operations.
- Added `H2` runtime dependency for in-memory testing and local validation.
- Added unit tests for task, habit, and wellness services.
- Added React + TypeScript frontend scaffold in `go/app-ui`.
- Completed frontend dependency installation and successful Vite production build.
- Fixed application property typo in `application.properties`.
